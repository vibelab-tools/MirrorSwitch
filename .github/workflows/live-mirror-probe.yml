name: Live mirror probe

on:
  schedule:
    - cron: "17 21 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cargo:
    name: Cargo catalog update and candidate probe
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - name: Install Rust toolchain
        run: rustup toolchain install 1.88.0 --profile minimal && rustup default 1.88.0
      - name: Build release executable
        run: cargo build --locked --release --bin mirrorswitch
      - name: Probe live catalog and Cargo candidates
        shell: bash
        run: |
          probe_cargo_home="$HOME/.mirrorswitch-live-cargo"
          mkdir -p "$probe_cargo_home"
          set +e
          CARGO_HOME="$probe_cargo_home" target/release/mirrorswitch plan --tool cargo --json > live-probe.json
          probe_status=$?
          set -e
          python3 - "$probe_status" <<'PY'
          import datetime
          import json
          import pathlib
          import sys

          path = pathlib.Path("live-probe.json")
          payload = json.loads(path.read_text())
          report = {
              "observed_at": datetime.datetime.now(datetime.timezone.utc).isoformat(),
              "exit_code": int(sys.argv[1]),
              "payload": payload,
          }
          path.write_text(json.dumps(report, indent=2) + "\n")
          if report["exit_code"] not in (0, 3):
              raise SystemExit(report["exit_code"])
          PY
      - name: Save timestamped probe evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: live-cargo-probe-${{ github.run_id }}
          path: live-probe.json
          if-no-files-found: error
          retention-days: 14
