name: Flutter macOS and Windows live boundary

on:
  schedule:
    - cron: "3 7 * * 3"
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_TOOLCHAIN: 1.88.0

jobs:
  native:
    name: Flutter (${{ matrix.name }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: macOS x86_64, runner: macos-15-intel }
          - { name: macOS arm64, runner: macos-15 }
          - { name: Windows x86_64, runner: windows-2025 }
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.47.2"
          channel: stable
          cache: false
      - name: Install Rust toolchain
        shell: bash
        run: |
          flutter --version
          rustup toolchain install "$RUST_TOOLCHAIN" --profile minimal
          rustup default "$RUST_TOOLCHAIN"
      - name: Restore shared Rust dependency cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: native-boundary
          add-job-id-key: "false"
          cache-bin: "false"
          cache-on-failure: "true"
      - name: Run deterministic Flutter boundary
        run: cargo test --locked --test flutter_adapter_boundary --test transaction_boundary --test restore_dispatch_boundary -- --test-threads=1
      - name: Build native release executable
        run: cargo build --locked --release --bin mirrorswitch
      - name: Exercise macOS profile, dual upstreams, frontends, and recovery
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -Eeuo pipefail
          binary="$PWD/target/release/mirrorswitch"; boundary="$RUNNER_TEMP/mirrorswitch-flutter-boundary"; export HOME="$boundary/home" SHELL=/bin/zsh
          unset PROFILE ZDOTDIR FLUTTER_STORAGE_BASE_URL PUB_HOSTED_URL PUB_CACHE
          project="$HOME/project"; profile="$HOME/.zshrc"; token_dir="$HOME/Library/Application Support/dart"; mkdir -p "$project" "$token_dir"
          printf '%s\n' '# native shell policy' "export FLUTTER_STORAGE_BASE_URL='https://storage.googleapis.com'" "export PUB_HOSTED_URL='https://pub.dev'" > "$profile"; chmod 0600 "$profile"
          printf '%s\n' '{"version":1,"hosted":[]}' > "$token_dir/pub-tokens.json"; chmod 0600 "$token_dir/pub-tokens.json"
          printf '%s\n' 'name: native_flutter' 'publish_to: none' 'environment:' "  sdk: '>=3.0.0 <4.0.0'" > "$project/pubspec.yaml"; chmod 0400 "$project/pubspec.yaml"
          printf '%s\n' 'packages: {}' > "$project/pubspec.lock"; chmod 0400 "$project/pubspec.lock"
          printf '{"version":1,"tools":[{"id":"flutter","scope":"user"}]}\n' > "$project/request.json"
          before=$(shasum -a 256 "$profile"|awk '{print $1}'); token_before=$(shasum -a 256 "$token_dir/pub-tokens.json"|awk '{print $1}'); project_before=$(shasum -a 256 "$project/pubspec.yaml"|awk '{print $1}'); lock_before=$(shasum -a 256 "$project/pubspec.lock"|awk '{print $1}'); mode=$(stat -f '%Lp' "$profile")
          cd "$project"
          "$binary" status --offline --json > status.json; "$binary" plan --offline --tool flutter --scope flutter=user --json > plan-cli.json; "$binary" plan --offline --config request.json --json > plan-config.json
          python3 - "$profile" <<'PY'
          import json, sys
          status=json.load(open('status.json')); tools=[t for t in status['detected_tools'] if t['adapter_key']=='flutter']; assert len(tools)==1
          evidence='\n'.join(tools[0]['detected']['evidence']); assert sys.argv[1] in evidence and 'Macos' in evidence and '--macos precache' in evidence and 'Pub token store file(s): 1' in evidence
          a=json.load(open('plan-cli.json')); b=json.load(open('plan-config.json')); assert a['ok'] and b['ok'] and a['plan']['tools'][0]['plan']==b['plan']['tools'][0]['plan']; assert len(a['plan']['tools'][0]['selection']['repositories'])==2
          PY
          printf '\nn\n'|"$binary" tui --offline --tool flutter --scope flutter=user > tui.txt; grep -E 'mirror=(nju|sjtug|tuna)' tui.txt
          "$binary" apply --offline --config request.json --yes --json > apply.json
          id=$(python3 - <<'PY'
          import json
          r=json.load(open('apply.json'))['result']; assert r['successful']; a=[t for t in r['tools'] if t['status']=='applied']; assert len(a)==1; print(a[0]['receipt']['transaction_id'])
          PY
          )
          grep -E "FLUTTER_STORAGE_BASE_URL='https://(mirrors\.nju\.edu\.cn/flutter|mirror\.sjtu\.edu\.cn)'" "$profile"; grep -E "PUB_HOSTED_URL='https://(mirror\.sjtu\.edu\.cn|mirrors\.tuna\.tsinghua\.edu\.cn)/dart-pub'" "$profile"
          "$binary" apply --offline --config request.json --yes --json > idempotent.json; "$binary" restore "$id" --yes --json > restore.json
          python3 - <<'PY'
          import json
          a=json.load(open('idempotent.json'))['result']; r=json.load(open('restore.json')); assert a['successful'] and not [t for t in a['tools'] if t['status']=='applied']; assert r['adapter_key']=='flutter' and r['receipt']['verified']
          PY
          test "$before" = "$(shasum -a 256 "$profile"|awk '{print $1}')"; test "$token_before" = "$(shasum -a 256 "$token_dir/pub-tokens.json"|awk '{print $1}')"; test "$project_before" = "$(shasum -a 256 pubspec.yaml|awk '{print $1}')"; test "$lock_before" = "$(shasum -a 256 pubspec.lock|awk '{print $1}')"; test "$mode" = "$(stat -f '%Lp' "$profile")"; test ! -e "$HOME/.mirrorswitch/verification/flutter/pubspec.yaml"; test ! -e "$HOME/.mirrorswitch/verification/flutter/pubspec.lock"
      - name: Exercise Windows launcher, registry pair, dual upstreams, and recovery
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference='Stop'; $binary=Join-Path $PWD 'target/release/mirrorswitch.exe'; $boundary=Join-Path $env:RUNNER_TEMP 'mirrorswitch-flutter-boundary'
          $env:USERPROFILE=Join-Path $boundary 'home'; $env:HOME=$env:USERPROFILE; $env:APPDATA=Join-Path $boundary 'AppData/Roaming'; $env:LOCALAPPDATA=Join-Path $boundary 'AppData/Local'; Remove-Item Env:FLUTTER_STORAGE_BASE_URL,Env:PUB_HOSTED_URL,Env:PUB_CACHE -ErrorAction SilentlyContinue
          $launcher=(Get-Command flutter -CommandType Application|Select-Object -First 1).Source; if([IO.Path]::GetExtension($launcher) -notin @('.bat','.cmd')){throw "native Flutter launcher is not a batch command: $launcher"}
          $project=Join-Path $env:USERPROFILE 'project'; $tokenDir=Join-Path $env:APPDATA 'dart'; New-Item -ItemType Directory -Force $env:USERPROFILE,$env:APPDATA,$env:LOCALAPPDATA,$project,$tokenDir|Out-Null
          & reg.exe add 'HKCU\Environment' /v FLUTTER_STORAGE_BASE_URL /t REG_SZ /d 'https://storage.googleapis.com' /f|Out-Null; & reg.exe add 'HKCU\Environment' /v PUB_HOSTED_URL /t REG_SZ /d 'https://pub.dev' /f|Out-Null
          [IO.File]::WriteAllText((Join-Path $tokenDir 'pub-tokens.json'),'{"version":1,"hosted":[]}',[Text.UTF8Encoding]::new($false)); [IO.File]::WriteAllText((Join-Path $project 'pubspec.yaml'),"name: native_flutter`r`npublish_to: none`r`nenvironment:`r`n  sdk: '>=3.0.0 <4.0.0'`r`n",[Text.UTF8Encoding]::new($false)); [IO.File]::WriteAllText((Join-Path $project 'pubspec.lock'),"packages: {}`r`n",[Text.UTF8Encoding]::new($false)); Set-ItemProperty (Join-Path $project 'pubspec.yaml') IsReadOnly $true; Set-ItemProperty (Join-Path $project 'pubspec.lock') IsReadOnly $true; [IO.File]::WriteAllText((Join-Path $project 'request.json'),'{"version":1,"tools":[{"id":"flutter","scope":"user"}]}',[Text.UTF8Encoding]::new($false))
          $token=Join-Path $tokenDir 'pub-tokens.json'; $tokenBefore=(Get-FileHash $token -Algorithm SHA256).Hash; $projectBefore=(Get-FileHash (Join-Path $project 'pubspec.yaml') -Algorithm SHA256).Hash; $lockBefore=(Get-FileHash (Join-Path $project 'pubspec.lock') -Algorithm SHA256).Hash; $tokenAcl=(Get-Acl $token).Sddl
          Push-Location $project
          try{
            (& $binary status --offline --json)|Set-Content -Encoding utf8 status.json; (& $binary plan --offline --tool flutter --scope flutter=user --json)|Set-Content -Encoding utf8 plan-cli.json; (& $binary plan --offline --config request.json --json)|Set-Content -Encoding utf8 plan-config.json
            $s=Get-Content status.json -Raw|ConvertFrom-Json; $tools=@($s.detected_tools|Where-Object adapter_key -eq 'flutter'); if($tools.Count -ne 1){throw 'Flutter not detected'}; $evidence=$tools[0].detected.evidence -join "`n"; if(-not $evidence.Contains('Windows') -or -not $evidence.Contains('--windows precache') -or -not $evidence.Contains('Windows user environment') -or -not $evidence.Contains('Pub token store file(s): 1')){throw 'native persistence/platform evidence missing'}
            $a=Get-Content plan-cli.json -Raw|ConvertFrom-Json; $b=Get-Content plan-config.json -Raw|ConvertFrom-Json; if(-not $a.ok -or -not $b.ok -or -not [string]::Equals([string]($a.plan.tools[0].plan|ConvertTo-Json -Depth 100 -Compress), [string]($b.plan.tools[0].plan|ConvertTo-Json -Depth 100 -Compress)) -or @($a.plan.tools[0].selection.repositories).Count -ne 2){throw 'frontend plans or repository composition differ'}
            @('','n')|& $binary tui --offline --tool flutter --scope flutter=user|Set-Content -Encoding utf8 tui.txt; if(-not((Get-Content tui.txt)-match 'mirror=(nju|sjtug|tuna)')){throw 'Flutter mirrors missing'}
            (& $binary apply --offline --config request.json --yes --json)|Set-Content -Encoding utf8 apply.json; $r=Get-Content apply.json -Raw|ConvertFrom-Json; if(-not $r.result.successful){throw 'Flutter apply failed'}; $applied=@($r.result.tools|Where-Object status -eq 'applied'); if($applied.Count -ne 1){throw 'transaction missing'}; $id=$applied[0].receipt.transaction_id
            $storage=(Get-ItemProperty 'HKCU:\Environment' FLUTTER_STORAGE_BASE_URL).FLUTTER_STORAGE_BASE_URL; $hosted=(Get-ItemProperty 'HKCU:\Environment' PUB_HOSTED_URL).PUB_HOSTED_URL; if($storage -notmatch '^https://(mirrors\.nju\.edu\.cn/flutter|mirror\.sjtu\.edu\.cn)$' -or $hosted -notmatch '^https://(mirror\.sjtu\.edu\.cn|mirrors\.tuna\.tsinghua\.edu\.cn)/dart-pub$'){throw 'registry pair mismatch'}
            (& $binary apply --offline --config request.json --yes --json)|Set-Content -Encoding utf8 idempotent.json; $again=Get-Content idempotent.json -Raw|ConvertFrom-Json; if(-not $again.result.successful -or @($again.result.tools|Where-Object status -eq 'applied').Count -ne 0){throw 'not idempotent'}; (& $binary restore $id --yes --json)|Set-Content -Encoding utf8 restore.json; $restore=Get-Content restore.json -Raw|ConvertFrom-Json; if(-not $restore.receipt.verified -or $restore.adapter_key -ne 'flutter'){throw 'restore failed'}
          }finally{Pop-Location}
          $storage=(Get-ItemProperty 'HKCU:\Environment' FLUTTER_STORAGE_BASE_URL).FLUTTER_STORAGE_BASE_URL; $hosted=(Get-ItemProperty 'HKCU:\Environment' PUB_HOSTED_URL).PUB_HOSTED_URL; $recovery=Join-Path $env:LOCALAPPDATA 'MirrorSwitch/flutter/environment-recovery.json'; $fixture=Join-Path $env:USERPROFILE '.mirrorswitch/verification/flutter/pubspec.yaml'; $managedLock=Join-Path $env:USERPROFILE '.mirrorswitch/verification/flutter/pubspec.lock'; if($storage -ne 'https://storage.googleapis.com' -or $hosted -ne 'https://pub.dev' -or $tokenBefore -ne (Get-FileHash $token -Algorithm SHA256).Hash -or $projectBefore -ne (Get-FileHash (Join-Path $project 'pubspec.yaml') -Algorithm SHA256).Hash -or $lockBefore -ne (Get-FileHash (Join-Path $project 'pubspec.lock') -Algorithm SHA256).Hash -or $tokenAcl -ne (Get-Acl $token).Sddl -or (Test-Path $recovery) -or (Test-Path $fixture) -or (Test-Path $managedLock)){throw 'Flutter state not restored exactly'}
      - name: Save redacted boundary evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: flutter-native-${{ matrix.name }}-${{ github.run_id }}
          path: ${{ runner.temp }}/mirrorswitch-flutter-boundary/home/project/*.json
          if-no-files-found: warn
          retention-days: 7
